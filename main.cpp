#include "lib.h"

#include <iostream>

namespace compare_nsp {
  inline bool is_close(const float a, const float b, const float epsilon = 0.00001) {
    throw "notImplemented";
  }
}

int main()
{
    std::cout<<"version {} was started"<<version();
    return 0;
}
